<DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<Grid Margin="8">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="2.2*" />
			<ColumnDefinition Width="6" />
			<ColumnDefinition Width="2.8*" />
		</Grid.ColumnDefinitions>

		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="8" />
			<RowDefinition Height="160" />
		</Grid.RowDefinitions>

		<!-- Tree -->
		<TreeView x:Name="NodeTree"
		          Grid.Row="0" Grid.Column="0"
		          ItemsSource="{Binding Roots}">
			<TreeView.ItemTemplate>
				<HierarchicalDataTemplate ItemsSource="{Binding Children}">
					<StackPanel Orientation="Horizontal">
						<TextBlock Text="{Binding Display}" />
						<TextBlock Text="  " />
						<TextBlock Text="{Binding TypeDisplay}" Opacity="0.6" />
					</StackPanel>
				</HierarchicalDataTemplate>
			</TreeView.ItemTemplate>
		</TreeView>

		<GridSplitter Grid.Row="0" Grid.Column="1"
		              HorizontalAlignment="Stretch"
		              VerticalAlignment="Stretch"
		              Width="6" />

		<!-- Detail panel -->
		<Border Grid.Row="0" Grid.Column="2"
		        Padding="8"
		        DataContext="{Binding SelectedItem, ElementName=NodeTree}">
			<DockPanel LastChildFill="True">
				<StackPanel DockPanel.Dock="Top">
					<TextBlock Text="{Binding Display}" FontWeight="SemiBold" FontSize="14" />
					<TextBlock Text="{Binding Kind}" Opacity="0.8" />
					<TextBlock Text="{Binding NodeType}" Opacity="0.8" />
					<TextBlock Text="{Binding TypeDisplay}" Opacity="0.8" Margin="0,0,0,8" />
				</StackPanel>

				<ItemsControl ItemsSource="{Binding Details}">
					<ItemsControl.ItemTemplate>
						<DataTemplate>
							<Grid Margin="0,2,0,2">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="Auto" />
									<ColumnDefinition Width="12" />
									<ColumnDefinition Width="*" />
								</Grid.ColumnDefinitions>
								<TextBlock Grid.Column="0" Text="{Binding Name}" FontWeight="SemiBold" />
								<TextBlock Grid.Column="2" Text="{Binding Value}" TextWrapping="Wrap" />
							</Grid>
						</DataTemplate>
					</ItemsControl.ItemTemplate>
				</ItemsControl>
			</DockPanel>
		</Border>

		<!-- Readable string -->
		<TextBox Grid.Row="2" Grid.ColumnSpan="3"
		         Text="{Binding ReadableText}"
		         IsReadOnly="True"
		         TextWrapping="Wrap"
		         AcceptsReturn="True"
		         VerticalScrollBarVisibility="Auto" />
	</Grid>
</DataTemplate>
